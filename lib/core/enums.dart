enum ConnectivityStatus {
  wiFi,
  mobileData,
  offline,
}

enum ThemeType {
  light,
  dark,
}

enum Gender {
  male,
  female,
}

enum MaritalStatus {
  single,
  married,
  widowed,
  divorced,
  separated,
}

enum ModelName {
  loginModel,
}

enum Level {
  one,
  two,
  three,
}

enum Zoom {
  zoomIn,
  zoomOut,
}

enum RowType { sum, avg, months, quarters, other }

enum FileType { video, photo, pdf, other }

enum RequestStatus { approved, rejected, pending }
